server:
  port: 9090

spring:
  application:
    name: basic
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3307/subscription?useUnicode=yes&characterEncoding=UTF-8&useSSL=false
    username: root
    password: root
    initialization-mode: always
    hikari:
      connection-timeout: 10000
      idle-timeout: 10000
      minimum-idle: 1
      maximum-pool-size: 10
  rabbitmq:
    host: localhost
    virtualHost: tmn-th-alpha
    port: 15672
    password: guest
    username: guest
    exchange: subs.exchange
    queue: subs.queue
    routingkey: subs.routingkey
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.H2Dialect
#        format_sql: true
#        id:
#          new_generator_mappings: false

subscription-service:
  api-key: "cb4532fc26b9421d88c5a079288bf51e"
  correlation-id-key: "X-Correlation-ID"
